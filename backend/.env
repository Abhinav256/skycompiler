PORT=4000
FRONTEND_ORIGIN=http://localhost:5173

# Set to "true" only once Docker images are built (npm run build:images)
# and the `docker` CLI is available to this process. Leave "false" for
# local development without Docker.
DOCKER_MODE=false

RUN_RATE_LIMIT=20
